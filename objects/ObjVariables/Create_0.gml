// Definición de variables

/*

COMENTARIOS

*/

// variable de instancia
objeto = 0       // variable de instancia (entero)
var_objeto = 0   // variable local (entero)
global.objeto = 0 // variable global (entero)

#macro CAM_VIEW_X 0 // macro (constante)

x = 0 // variable de instancia (entero)

arr[0] = 0 // elemento de un arreglo (entero)
arr_b = [] // arreglo vacío (arreglo)

for (var i = 0; i < 10; i++) { 
    arr[i] = irandom(10) // elemento de un arreglo (entero)
}

// estructura de datos
str = { 
    item: "espada",    // propiedad de objeto (cadena)
    cantidad: 0,       // propiedad de objeto (entero)
}
